# 基于扰动标量翻转的投毒攻击

## 背景

主动攻击可篡改模型训练期间服务器与客户端之间的交互信息，然后通过观察模型反馈展开攻击操作。该方式会损伤模型精度且引入额外时空代价，如常用的梯度上升方式会误导模型收敛方向、减缓收敛速度，但由于投毒的精准性，可获取更有价值的启发信息以提高攻击准确率。联邦学习过程中客户端可随机参与各通信轮次训练。在推理扰动模式下，服务器端会缓存大量扰动序列以便各客户端参与训练时可按需参更新本地模型。服务器亦可直接指定客户端在本地训练期间的种子选择。丰富的缓存信息和灵活的协作方式，为均衡攻击效果与模型性能损失提供了新的契机。为此，拟针对扰动序列格式，通过种子选择和缓存标量翻转的方式，分别从扰动方向和扰动幅度两个角度，探索新型主动攻击方案，在强化攻击效果的同时，控制模型性能损失。

## 研究内容

联邦学习过程中，每个通信轮次仅有部分客户端被选中参与模型训练，以保证通信开销的最小化和训练效率的提高。因此，服务器必须缓存所有历史通信轮次的更新信息，以便被选中的客户端可据此将本地参数从上一次训练的版本快速更新至最新版本，从而正常参与新一轮训练。在推理扰动模式下，缓存信息为扰动序列集合。考虑到传统梯度上升攻击手段，会计算目标样本梯度，使用其相反值恶意更新参数，并根据后续训练是否尝试修正上述投毒梯度，判断训练集中是否包含目标样本。本申请拟将该方法迁移至扰动序列，通过对表征参数更新幅度的标量值投毒，展开攻击；拟设计优先级投毒筛选机制，首先分析缓存序列中的种子与新一轮训练选定种子之间的相似性，对高相似性种子的缓存标量进行投毒，以保证投毒方向和修正方向的一致性，进而保证攻击效果，其次对比目标样本在候选投毒种子上的标量反应值，量化其对原缓存标量的影响能力，优先选择低影响度标量进行反应值翻转，以降低投毒导致的模型精度损失。

## 拟解决的关键科学问题

**（1）模型收敛过程中对不同样本预测行为差异性的有效识别问题。**

联邦环境下，被动攻击需要收集模型在收敛过程中对训练集成员和非训练集成员样本的预测行为，然后展开对比分析。其依据的原理是基于成员样本训练的模型会对前者表现出过拟合现象。然而，AI领域的正则化损失函数设计和联邦学习环境下均衡不同客户端更新信息等优化机制，均旨在避免过拟合、提高泛化性。这急剧增加了预测行为差异性的识别难度。同时，考虑到迭代训练的本质是对模型的预测行为进行持续修正，所以预测行为的差异性会随迭代步而动态变化，甚至前后矛盾。因此，对于已经识别的差异现象，如何判断其有效性，对于提高攻击准确率至关重要。

**（2）推理扰动模式下面向众多扰动方向的最优攻击角度选择问题。**

干扰与监测是展开主动攻击的两个步骤，即先干扰模型的正常收敛方向、然后监测后续训练的修复行为。在传统反向传播模式下，梯度作为干扰对象，与样本和模型参数直接关联，三者逻辑关系清晰。但在推理扰动模式下，作为干扰对象的扰动标量，需要依赖扰动方向才能对梯度进行近似还原，进而更新参数，这种间接关联使得干扰效果的可控性差。进一步地，串行的干扰-监测机制无法在一个迭代步内同时完成，但每个迭代步均需要从多个扰动方向展开标量计算且扰动方向随迭代步而动态变化。如何从每个迭代步内的众多候选对象中选定干扰目标，同时跨迭代步对齐干扰与监测行为，是保证主动攻击效果的关键所在。

## 方法设计

现有主动投毒工作均面向传统反向梯度计算模式，采用粗粒度的全局梯度扰动策略，未能充分利用联邦学习中的结构特征，导致攻击效果与模型性能的平衡难以把控。为均衡两者之间的矛盾，本申请着重研究联邦推理扰动模式下的累积更新序列特征，设计了一种兼顾攻击效果和模型性能的精细化投毒机制。

在联邦学习框架下，由于客户端参与训练的概率往往较低，服务器需要维护并按需下发累积的模型更新序列，以帮助新加入训练的客户端快速将本地模型更新为最新版本。具体地，假设$T_{a}$为客户端$C_{i}$上次参与的全局训练轮次，而在$T_{b}$轮次$C_{i}$再次被选中参加训练，如图左部所示，由于$C_{i}$未参与$T_{a}$至$T_{b}$之间的训练过程，服务器将下发该期间累积的更新序列$\{\lbrack s,\ v\rbrack,...\}$，其中$s$表示随机种子，$v$表示对应的标量更新。这一机制为实施精细化成员推断攻击提供了契机。具体地，在下发累积序列时，保持种子$s$不变，但对特定的标量$v$进行恶意篡改。如果目标样本$(x,y)$确实存在于客户端的训练数据中，投毒后的参数将在该样本上产生较大的损失值，进而在后续本地训练中产生显著的修复校正行为，也即在推理扰动框架下的大标量响应值。通过观察这种修复行为，服务器可以推断目标样本的归属。

然而，投毒不可避免地会影响模型的性能表现。为此，本申请拟设计优先级投毒筛选机制，以实现攻击效果和模型性能的均衡。如图所示，为了保证投毒方向和修正方向的一致性，进而保证攻击效果，首先需要分析缓存序列中的种子与新一轮训练选定种子之间的相似性，对高相似性种子的缓存标量进行投毒。也即，如果存在缓存的第$T_{j}$通信轮次下客户端$C_{i}$选中的某个种子$s_{i^{\ast}}^{j}$与第$T_{b + 1}$轮中将要使用的某个种子$s_{i^{\ast}}^{b + 1}$相当，则直接将缓存的扰动对[$s_{i^{\ast}}^{j}$, $v_{i^{\ast}}^{j}$]放入备选投毒集合。若两者不相等，亦可使用对应的高斯扰动向量余弦相似度$cos(\overrightarrow{z_{i}},\overrightarrow{z_{j}}) = \overrightarrow{z_{i}} \bullet \overrightarrow{z_{j}}/(|\overrightarrow{z_{i}}||\overrightarrow{z_{j}}|)$计算，量化相似度。通过设定相似度判定阈值$\tau$进行Top-*k*备选种子筛选。另一方面，为了降低投毒导致的模型精度损失，拟根据标量影响进行评估。具体地，定义标量$v_{i^{\ast}}^{j}$反应值$\rho_{j} = |v_{i^{\ast}}^{j}(x,y)|/|v_{i^{\ast}}^{j}|$，其中目标样本上的标量计算方式如下：

$$
v_{i^{\ast}}^{j}(x,y) = (L(\theta_{i}^{j} + \epsilon\overrightarrow{z}) - L(\theta_{i}^{j}\mathbf{\ } - \ \epsilon\overrightarrow{z}))/2\epsilon
$$

这里，$\theta_{i}^{j}$为全局模型参数，$L(\theta)$表示模型$\theta$在目标样本$(x,y)$上计算得到的损失。选择$\rho_{j}$较小的项实施投毒，将其标量篡改为。如图右侧$v_{i^{\ast}}^{j}{'} = v_{i^{\ast}}^{j} - v_{i^{\ast}}^{j}(x,y)/v_{i^{\ast}}^{j}$所示，当投毒序列下发给目标客户端后，客户端$C_{i}$首先据此进行版本更新，随后进行本地训练并上报新的扰动序列。若目标样本存在，将观察到明显的损失上升和后续的修复性波动；反之，则不会出现显著的响应。这种响应模式的差异为成员推断提供了可靠的判据。为进一步提升攻击效果，本申请拟引入多轮投毒观察机制，通过连续监测客户端的更新行为分析其响应模式。若连续观察到修复波动，则可高置信度地判定目标样本存在。

![img](img/wps33.png)



## 算法设计

1.扰动序列相似度计算算法

```python
算法 1: 扰动序列相似度计算(SimilarityMeasurement)
输入: 
    历史扰动序列集合 H = {[s_i, v_i]}
    当前轮次种子集合 S_current
    相似度阈值 τ
输出: 
    候选投毒序列集合 C

1: C ← ∅
2: for each [s_i, v_i] in H do
3:    for each s_j in S_current do
4:        if s_i == s_j then
5:            C ← C ∪ {[s_i, v_i]}
6:            continue
7:        z_i ← GaussianNoise(s_i)  // 生成高斯噪声向量
8:        z_j ← GaussianNoise(s_j)
9:        sim ← cosine_similarity(z_i, z_j)
10:       if sim > τ then
11:           C ← C ∪ {[s_i, v_i]} 
12: return C
```

2.优先级投毒标量选择算法

```python
算法 2: 优先级投毒标量选择(PoisonScalarSelection) 
输入:
    候选投毒序列集合 C
    目标样本 (x, y)
    全局模型参数 θ
    扰动幅度 ε
    选择数量 k
输出:
    最终投毒序列集合 P

1: P ← ∅
2: scores ← []
3: for each [s_i, v_i] in C do
4:    z_i ← GaussianNoise(s_i)
5:    L_pos ← Loss(θ + ε·z_i, x, y)
6:    L_neg ← Loss(θ - ε·z_i, x, y)
7:    v_target ← (L_pos - L_neg)/(2ε)
8:    ρ_i ← |v_target|/|v_i|+|v_target|
9:    scores.append([ρ_i, s_i, v_i])
10: sort scores by ρ_i in ascending order
11: for i in range(k) do
12:    [ρ, s, v] ← scores[i]
13:    v_poisoned ← v - v_target/v
14:    P ← P ∪ {[s, v_poisoned]}
15: return P
```

3.多轮投毒观察与成员身份推断算法

```python
算法 3: 多轮投毒观察与成员身份推断(MembershipInference)
输入:
    观察轮数 T
    客户端响应序列集合 R = {R_1,...,R_T}
    波动阈值 δ
    置信度阈值 η
输出:
    成员身份判定结果 result

1: count ← 0
2: prev_loss ← 0
3: for t in range(T) do
4:    current_loss ← ComputeLoss(R_t)
5:    if t == 0 then
6:        if current_loss > prev_loss + δ then
7:            count += 1
8:    else:
9:        fluctuation ← |current_loss - prev_loss|
10:       if fluctuation > δ then
11:           count += 1
12:    prev_loss ← current_loss
13: confidence ← count/T
14: if confidence > η then
15:    result ← "Member"
16: else:
17:    result ← "Non-member"
18: return result
```

