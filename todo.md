# 成员推理攻击实现要求

# （1）推理目标数据是否属于全体训练集
## 数据集准备
采用 databricks-dolly-15k.jsonl 数据集选取部分数据进行联邦训练
将数据集中的数据按照8:2的比例划分为训练集和测试集

## 客户端训练数据划分
将训练集的数据划分给各个客户端

## 攻击目标数据准备
在训练集和测试集中分布抽取(10)条数据构成攻击目标数据

## 服务器端控制
- 攻击者拥有服务器端的控制权
服务器维持一个种子标量池，负责对全局模型的更新：全局模型(w) = 初始模型(w0) + sum(v_i*z_i)。其中v_i是种子标量池中的种子的梯度标量，z_i是由种子s_i生成的维度和w0相同的高斯向量。
- 每轮训练时：
  服务器选取特定比例的客户端参加本轮训练，每隔T轮进行一次投毒
  1. 服务器指定客户端训练的种子
  2. 如果进行攻击，服务器会篡改全局种子标量池：
     - 筛选与该轮次指定客户端训练的种子重合或相似的种子（k1个）
     - 根据优先级从k1个种子中筛选出k2个
     - 对这k2个种子的标量进行篡改
  3. 将初始模型加上篡改后的种子标量池更新的模型发给客户端

## 客户端训练
若客户端参与本轮训练，则会接收到服务器下发的当前全局模型和客户端本轮训练的候选种子
客户端利用本地训练集和候选种子对全局模型进行多轮本地微调训练
每次本地训练都在候选种子中抽取一个进行零阶优化微调，得到对应的梯度标量
- 客户端训练后上传多轮的种子梯度标量
- 服务器根据目标客户端上传的多轮种子标量信息更新全局种子标量池

## 聚合评估
- 服务器收到客户端上传的梯度标量后，进行聚合
- 聚合后，服务器将聚合后的梯度标量更新到全局模型
- 使用更新后的全局模型计算在各目标数据上的loss，并记录loss和对应的轮次


<!-- # （2）在推理目标数据是否属于全体训练集的基础上,推理目标数据是否属于目标客户端的训练集
## 数据集准备
采用 databricks-dolly-15k.jsonl 数据集进行联邦训练
将数据集中的数据按照8:2的比例划分为训练集和测试集

## 客户端训练数据划分
将训练集的数据划分给各个客户端
记录攻击的目标客户端的训练数据

## 攻击目标数据准备
抽取目标客户端训练数据的5%作为攻击目标数据
再从其他数据中随机选取等量数据
二者合并作为攻击目标数据
- 对这批数据进行投毒攻击
- 最后统计该成员推理攻击的正确率

## 服务器端控制
- 攻击者拥有服务器端的控制权
- 每轮训练时：
  服务器选取特定数量的客户端参加本轮训练
  如果执行攻击，则必定抽取攻击目标客户端参加每轮训练
  1. 服务器指定客户端训练的种子
  2. 如果进行攻击，服务器会篡改全局种子标量池：
     - 筛选与该轮次指定客户端训练的种子重合或相似的种子（k1个）
     - 根据优先级从k1个种子中筛选出k2个
     - 对这k2个种子的标量进行篡改
  3. 将初始模型加上篡改后的种子标量池更新的模型发给客户端

## 客户端训练
若客户端参与本轮训练，则会接收到服务器下发的当前全局模型和客户端本轮训练的候选种子
客户端利用本地训练集和候选种子对全局模型进行多轮本地微调训练
每次本地训练都在候选种子中抽取一个进行零阶优化微调，得到对应的梯度标量
- 客户端训练后上传多轮的种子梯度标量
- 服务器根据目标客户端上传的多轮种子标量信息更新全局种子标量池

## 聚合评估
- 服务器收到客户端上传的梯度标量后，进行聚合
- 聚合后，服务器将聚合后的梯度标量更新到全局模型
- 服务器对目标客户端进行成员推理攻击

## 训练设定修改
- 如果实施攻击，每轮参与训练的客户端不再随机选择
- 攻击的目标客户端必须参与训练，其他客户端随机选择
- 设定投毒间隔参数T：每隔T轮服务器进行一次投毒
- 通过观察投毒后的T轮全局模型在目标数据上的loss变化判断是否是目标客户端的成员

## 数据记录
- 将要攻击的数据保存在额外的文件中
- 记录目标客户端训练集是否包含该数据，用于判断最终攻击结果的正确性
 -->

